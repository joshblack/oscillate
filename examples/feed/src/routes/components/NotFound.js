import React from 'react';

const NotFound = () => (
  <div className="App">
    <h1>Whoops!</h1>
    <p>
      Looks like there isn't an example for the given route.
      Feel free to try clicking one of the links at the top of the page.
    </p>
  </div>
);

export default NotFound;
